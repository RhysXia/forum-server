spring:
  application:
    name: forum-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RhysXia/forum-server
          search-paths: config
server:
  port: 8200

